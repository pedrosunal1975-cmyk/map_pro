# XML Validator - Minimal Requirements
# ======================================
# 
# These are the ONLY dependencies needed for the XML validator.
# Your existing requirements.txt already includes these!

# Core XML processing
lxml>=4.9.0

# CLI interface (optional, only for CLI usage)
rich>=13.5.0

# Testing (optional, only for development)
pytest>=7.4.0
pytest-cov>=4.1.0
